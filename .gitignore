# Python
__pycache__/
*.pyc
*.pyo
server/.venv/
server/.installed

# OS
.DS_Store
Thumbs.db
node_modules/

# Tauri
src-tauri/target/
dist/
_shots/

# Parity: the real-season golden holds the coach's real data (PII) — local only.
# The synthetic-edge golden IS committed (reproducible on CI).
tools/parity-golden/mavericks-6game.json

# Personal documents that live in this working tree but must NEVER be committed
# (a resume generator carrying phone/email/employment history was swept into
# fc00ce0 by a `git add -A` and had to be untracked + purged from history).
*_Resume*.docx
*Resume*.pdf
tools/build_charles_kelly_resume.py
resume_qa/
resume_qa_rewritten/

# S6-6: scratch captures, probe scripts and gate logs are LOCAL ONLY. Several
# contain frames of the coach's real team film and screenshots of real season
# data. A `git add -A` swept 267 of them into 7477fe2; this stops a recurrence.
.tmp-*
_shots-*/

# Machine-local scratch. AGENTS.md is NOT here: it is shared repository
# guidance and is version-controlled (2026-09-02).
tmpdur.mjs
tools/.tmp-*
# Real film frames are never committed (see design-comps README).
design-comps/**/assets/film-frame.*

# Local forensic snapshots may contain full seasons and film.
incident-backups/

# Generated or machine-local output, anchored to the repository root so a
# same-named directory deeper in the tree is never silently hidden.
# .claude/ is machine-local agent config.
/.claude/
# artifacts/ and scratchpad/ hold capture output, probes and gate logs; several
# contain real season data and frames of the coach's team film.
/artifacts/
/scratchpad/
# Built Windows installers.
/GridIron-IQ-*-Beta-Setup.exe
